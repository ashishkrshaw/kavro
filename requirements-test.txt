# Test dependencies for Kavro
# Install with: pip install -r requirements-test.txt

# Testing framework
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Async test client
httpx==0.28.1

# SQLite async for testing (lighter than PostgreSQL)
aiosqlite==0.20.0
